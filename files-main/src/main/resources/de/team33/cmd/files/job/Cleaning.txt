Your request:

    %1$s

Expected request scheme:

    %2$s clean PATH

    ... to remove empty directories within a specific directory.

Parameters:

    PATH - A path to be examined. Subdirectories are included if existing.