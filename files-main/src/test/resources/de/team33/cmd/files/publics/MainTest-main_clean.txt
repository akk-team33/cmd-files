+------------------------------------------------------------------------------
| ${project.groupId} - ${project.artifactId} - version ${project.version}
+------------------------------------------------------------------------------
Your request:

    MainTest clean

Expected request scheme:

    MainTest clean PATH [PATH [...]]

    ... to remove given directories if they are empty
        after cleaning all their subdirectories.

Parameters:

    PATH   - A directory path to be examined. Multiple PATHs are possible.%n%n